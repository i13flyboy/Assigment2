BoxLayout:
    orientation: 'horizontal'
    BoxLayout:
        orientation: 'vertical'
        # Left hand column
        size_hint_x: 0.3
        Label:
            text: "Sort by:"
        Spinner:
            id: select_day
            values: app.current_place
            on_text: app.change_status(self.text)
        Label:
            id: output_label
        Label:
            text: "Add New Places"
        Label:
            text: "Place"
        TextInput:
            id: new_place
            multiline: False
            write_tab: False
        Label:
            text: "Country"
        TextInput:
            id: new_country
            multiline: False
            write_tab: False
        Label:
            text: "Priority"
        TextInput:
            id: new_priority
            multiline: False
            write_tab: False
        Button:
            on_press: app.add_new_place()
            text: "Add Place"
        Button:
            text: "Clear"
            on_press: app.handle_clear()
    BoxLayout:
        # Right hand column
        orientation: 'vertical'
        Label:
            size_hint_y: 0.2
            text: 'Places to visit'
        BoxLayout:
            orientation: 'vertical'
            id: box_list
        Label:
            size_hint_y: 0.1
            text: app.program_status_bar
        BoxLayout:
            orientation: 'vertical'
            id: place_buttons
        Label:
            id: status_visited
            text: status_visited
            size_hint_y: 0.1
            text: 'Status visited'

